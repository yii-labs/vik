loop:
  max_issue_concurrency: 2
  wait_ms: 5000

workspace:
  root: .vik

agents:
  coder:
    runtime: codex
    model: gpt-5.5

issues:
  pull:
    command: |
__PULL_COMMAND__
    idle_sec: __IDLE_SEC__

issue:
  stages:
__STAGES__
