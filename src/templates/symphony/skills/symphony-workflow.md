---
name: symphony-workflow
description: Use for Symphony-style Vik stages that move work from plan/rework through work, review, and merge.
---

# Symphony Workflow

Keep one issue moving through one explicit stage at a time.

## Rules

- Read tracker detail before stage work.
- Keep comments current with exact commands and results.
- Move state only after stage work and validation finish.
- Use `rework` for review-requested fixes that need another implementation pass.
- Link pull requests with the tracker issue before merge.
- Refresh this bundled skill with `vik init --force` when you want the latest template copy.
